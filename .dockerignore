# Git
.git
.gitignore

# Node modules (устанавливаются внутри контейнера)
node_modules
**/node_modules

# Dev/build артефакты
**/dist
**/.tsbuildinfo

# Логи
*.log

# Среда разработки
.local
.replit
replit.nix

# Секреты (никогда не включать в образ)
.env
deploy/.env
**/.env.local

# Certbot (создаётся от root, недоступен для Docker build context)
deploy/certbot/

# Загруженные файлы (не нужны в образе)
attached_assets/
